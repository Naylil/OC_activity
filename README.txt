This project aims to display data retrieved by a camera using MATLAB scripts.

plot_all : main script
compose_rotation : function used to estimate points in camera frame
plotXYZRTL : function which uses compose_rotation to plot the points
plotcune : function used to plot a cube (the camera)